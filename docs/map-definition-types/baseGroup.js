/** @import { BaseHex } from './baseHex.js' */

/**
 * @typedef {object} BaseGroup - base group (for one player) definition
 * @property {number} forPlayersCount - for how many players is this base designed
 * @property {BaseHex[]} hexes - all base hex definitions
 */
