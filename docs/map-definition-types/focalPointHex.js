/** @import { HexCoords } from './hexCoords.js' */

/**
 * @typedef {object} FocalPointHex - focal point hex definition
 * @property {HexCoords} coords - focal point hex coordinates
 * @property {boolean} isActive - at least one focal point in a group must start as active
 */
