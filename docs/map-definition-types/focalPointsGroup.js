/** @import { FocalPointHex } from './focalPointHex.js' */

/**
 * @typedef {object} FocalPointsGroup - focal point group definition
 * @property {number} forPlayersCount - for how many players is this focal points group designed
 * @property {FocalPointHex[]} hexes - all focal points hexes definitions
 */
